# コーディング思考プロセスガイド

## UI実装のステップバイステップ思考法

FlutterでUIを作る時、以下の思考プロセスで進めると効率的です。

### ステップ1: 目標のUIを視覚化する

まず、完成形のUIを明確にイメージします。

**確認事項:**

- どのような要素が必要か？（テキスト、ボタン、画像など）
- 要素はどのように配置されているか？（縦並び、横並び、重なり）
- どの要素が動的に変化するか？（ユーザー操作による変化）

**実践例:**

```
目標: ログイン画面を作成する
必要な要素:
- メールアドレス入力欄
- パスワード入力欄
- ログインボタン
配置: 縦に並べる
動的変化: 入力内容を取得する必要がある
```

### ステップ2: レイアウト構造を設計する

視覚化したUIをウィジェットツリーとして構造化します。

**設計のヒント:**

```
例: ログイン画面の構造
Scaffold
├── AppBar (タイトル表示)
└── body: SingleChildScrollView (キーボード対応)
    └── Padding (余白)
        └── Column (縦並び)
            ├── TextField (メール入力)
            ├── TextField (パスワード入力)
            └── ElevatedButton (ログインボタン)
```

**レイアウト選択の判断基準:**

- 縦に並べる → Column
- 横に並べる → Row
- 重ねて配置 → Stack
- スクロールが必要 → SingleChildScrollView / ListView
- 中央揃え → Center

### ステップ3: 外側から内側へ実装する

大きなコンテナから実装を始め、徐々に中身を追加します。

**実装順序の例:**

1. まずScaffoldを作成
2. bodyにColumnを配置
3. Columnの中にTextFieldを1つ追加
4. 動作確認してから次のTextFieldを追加
5. 最後にボタンを追加

**メリット:**

- 各段階で動作確認ができる
- エラーが発生しても原因を特定しやすい
- 段階的に進めるので心理的ハードルが低い

**コード例:**

```dart
// ステップ1: Scaffold
Scaffold(
  appBar: AppBar(title: Text('ログイン')),
  body: Container(), // まず空で作成
)

// ステップ2: Columnを追加
Scaffold(
  appBar: AppBar(title: Text('ログイン')),
  body: Column(
    children: [],
  ),
)

// ステップ3: TextFieldを1つ追加
Scaffold(
  appBar: AppBar(title: Text('ログイン')),
  body: Column(
    children: [
      TextField(),
    ],
  ),
)

// ステップ4: 残りの要素を追加
Scaffold(
  appBar: AppBar(title: Text('ログイン')),
  body: Column(
    children: [
      TextField(),
      TextField(),
      ElevatedButton(onPressed: () {}, child: Text('ログイン')),
    ],
  ),
)
```

### ステップ4: 状態管理を考える

動的に変化する要素がある場合、StatefulWidgetを使用するか検討します。

**StatefulWidgetが必要なケース:**

- ユーザー入力を受け付ける（TextField）
- ボタンを押してUIを変化させる
- カウンタ、チェックボックス、スイッチなど
- 入力内容を保持する必要がある

**StatelessWidgetで十分なケース:**

- 静的なテキスト表示
- 固定されたレイアウト
- 親ウィジェットがStatefulWidgetの場合

**判断フロー:**

```
UIが変わるか？
├── はい → StatefulWidget
│   ├── ユーザー入力が必要？ → TextEditingControllerを使用
│   ├── ボタンで変化？ → setState()を使用
│   └── データが変わる？ → setState()を使用
└── いいえ → StatelessWidget
```

### ステップ5: プロパティを調整する

基本構造ができたら、見た目を調整します。

**調整の順序:**

1. サイズ（width, height）
2. 余白（padding, margin）
3. 色（color, backgroundColor）
4. 配置（alignment, mainAxisAlignment）
5. 装飾（border, borderRadius, boxShadow）

**実践例:**

```dart
// 基本構造
ElevatedButton(
  onPressed: () {},
  child: Text('ログイン'),
)

// サイズ調整
SizedBox(
  width: double.infinity,
  height: 50,
  child: ElevatedButton(
    onPressed: () {},
    child: Text('ログイン'),
  ),
)

// スタイル調整
SizedBox(
  width: double.infinity,
  height: 50,
  child: ElevatedButton(
    onPressed: () {},
    style: ElevatedButton.styleFrom(
      backgroundColor: Colors.blue,
      foregroundColor: Colors.white,
    ),
    child: Text('ログイン'),
  ),
)
```

## ウィジェット選択の思考プロセス

「どのウィジェットを使えばいいか」分からない場合の判断フロー:

```
何を表示したいか？
├── テキスト → Text
├── 画像 → Image
├── ボタン → ElevatedButton / TextButton / IconButton
├── 入力欄 → TextField
├── リスト → ListView / GridView
└── その他
    ├── コンテナが必要 → Container
    ├── アイコン → Icon
    └── カード風に → Card
```

**ボタンの選択基準:**

- 主要なアクション → ElevatedButton（影付き）
- 補助的なアクション → TextButton（テキストのみ）
- アイコンのみ → IconButton
- ナビゲーション → FloatingActionButton

## エラー発生時の思考プロセス

エラーが出た場合、以下の手順で対処します。

### 1. エラーメッセージを読む

エラーメッセージには以下の情報が含まれています:

- エラーの種類: 何が問題か（型不一致、未定義など）
- ファイル名と行番号: どこでエラーが発生しているか
- 期待される値: 何が期待されているか

**読む順序:**

1. まずエラーの種類を理解
2. ファイル名と行番号で該当箇所を特定
3. 期待される値を確認し、現在のコードと比較

### 2. 原因を推測する

よくある原因を確認します:

- 変数の型が間違っていないか？
- ウィジェットのプロパティ名が正しいか？
- 必要なインポートが不足していないか？
- ウィジェットのネストが正しいか？

### 3. 公式ドキュメントやウィジェットガイドを参照

- ウィジェットの正しい使い方を確認
- プロパティの期待される型を確認
- サンプルコードを参考にする

### 4. 最小限のコードでテスト

問題の箇所を単純化してテスト:

- 問題の箇所だけを切り出して新しいファイルに作成
- そのコードだけでエラーが再現するか確認
- 原因を特定したら元のコードに戻して修正

## 実践的な学習方法

### ガイドを見ながら実装する段階

この段階では、ガイドを参考にしながら実装します。

**手順:**

1. ウィジェットガイドから必要なウィジェットを探す
2. サンプルコードをコピーして貼り付け
3. プロパティを自分の用途に合わせて修正
4. 動作確認

**目標:**

- ウィジェットの基本的な使い方を理解する
- プロパティの役割を覚える

### 徐々にガイドに頼らない段階

この段階では、まず自分で書いてみて、必要な時のみガイドを参照します。

**手順:**

1. まず自分で書いてみる
2. うまくいかない場合のみガイドを参照
3. ガイドを見て理解したら、自分で書き直す
4. 同じウィジェットを何度も使って定着させる

**目標:**

- ウィジェット名を覚える
- よく使うプロパティを暗記する

### 「一から書ける」ようになるための練習

この段階では、ガイドを見ずに実装できるように練習します。

**練習方法:**

1. 簡単なUIを何度も繰り返し実装する
2. ガイドを見ずに書いてみて、後で答え合わせをする
3. よく使うウィジェットの組み合わせをパターンとして覚える

**具体的な練習課題:**

- テキストとボタンを並べる画面を3回作る
- 入力欄とボタンを並べる画面を3回作る
- リストを表示する画面を3回作る

**パターン化の例:**

```
ボタン付きテキストのパターン:
Column(
  children: [
    Text('タイトル'),
    SizedBox(height: 16),
    ElevatedButton(
      onPressed: () {},
      child: Text('ボタン'),
    ),
  ],
)
```

## よく使うウィジェットの組み合わせパターン

### パターン1: タイトルと説明文

```dart
Column(
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [
    Text(
      'タイトル',
      style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
    ),
    SizedBox(height: 8),
    Text(
      '説明文',
      style: TextStyle(fontSize: 16, color: Colors.grey),
    ),
  ],
)
```

### パターン2: 入力欄とボタン

```dart
Column(
  children: [
    TextField(
      decoration: InputDecoration(
        labelText: 'ラベル',
        border: OutlineInputBorder(),
      ),
    ),
    SizedBox(height: 16),
    SizedBox(
      width: double.infinity,
      child: ElevatedButton(
        onPressed: () {},
        child: Text('送信'),
      ),
    ),
  ],
)
```

### パターン3: リストアイテム

```dart
Card(
  child: ListTile(
    leading: Icon(Icons.icon),
    title: Text('タイトル'),
    subtitle: Text('サブタイトル'),
    trailing: Icon(Icons.chevron_right),
    onTap: () {},
  ),
)
```

### パターン4: 中央揃えのコンテンツ

```dart
Center(
  child: Column(
    mainAxisAlignment: MainAxisAlignment.center,
    children: [
      Icon(Icons.icon, size: 64),
      SizedBox(height: 16),
      Text('メッセージ'),
    ],
  ),
)
```

## 効率的なコーディング習得のためのヒント

### 1. 小さく始める

最初から複雑なUIを作ろうとせず、簡単なUIから始めます。

**推奨される順序:**

1. テキストを表示するだけの画面
2. ボタンを追加する画面
3. 入力欄を追加する画面
4. リストを表示する画面

### 2. 同じことを繰り返す

同じウィジェットやパターンを何度も使うことで、定着させます。

**繰り返しの効果:**

- ウィジェット名が自然に出てくるようになる
- プロパティの使い方が体で覚えられる
- エラーに対する対処法が身につく

### 3. エラーを恐れない

エラーは学習の機会です。エラーが出たら、原因を理解することが重要です。

**エラーへの対処:**

- エラーメッセージをよく読む
- 原因を特定する
- 解決策を調べる
- 次回同じエラーが出ないように覚える

### 4. 公式ドキュメントを活用する

Flutterの公式ドキュメントは非常に充実しています。

**活用方法:**

- ウィジェットの詳細を確認する
- サンプルコードを参考にする
- APIドキュメントで仕様を確認する

---

**ナビゲーション:**

- ← 前へ: [ウィジェット基礎演習](./ウィジェット基礎演習.md)
- [学習教材インデックスに戻る](./INDEX.md)
- 次へ: [コードリーディングガイド](./コードリーディングガイド.md) →

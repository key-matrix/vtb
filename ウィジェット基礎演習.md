# 実践演習問題

## このガイドの目的

このガイドは、ガイドを見ずにコードを書けるようになるための実践的な演習問題を提供します。「ガイドを見ないと書けない」という悩みを解決するために、段階的な難易度の問題を用意しました。

---

## 演習の進め方

1. **ガイドを見ずに挑戦**: まずはガイドを見ずに自分で書いてみる
2. **答え合わせ**: うまくいかない場合のみ答えを確認
3. **理解して再挑戦**: 答えを見て理解したら、もう一度自分で書く
4. **繰り返し練習**: 同じ問題を複数回解いて定着させる

---

## レベル1: 基礎演習（ガイドを見ながらでもOK）

### 問題1-1: シンプルなテキスト表示

**要件:**

- 画面中央に「Hello, Flutter!」というテキストを表示する

**ヒント:**

- Centerウィジェットを使用
- Textウィジェットを使用

**答え:**

```dart
Center(
  child: Text('Hello, Flutter!'),
)
```

---

### 問題1-2: ボタンを表示

**要件:**

- 画面中央に「Click me」というテキストのボタンを表示する
- ボタンを押したらコンソールに「Button pressed!」と表示する

**ヒント:**

- ElevatedButtonを使用
- onPressedにprintを記述

**答え:**

```dart
Center(
  child: ElevatedButton(
    onPressed: () {
      print('Button pressed!');
      //もしくは debugPrint を使用()
    },
    child: Text('Click me'),
  ),
)
```

---

### 問題1-3: 縦に並べる

**要件:**

- 3つのテキスト（「Item 1」「Item 2」「Item 3」）を縦に並べて表示する

**ヒント:**

- Columnを使用
- childrenにTextを3つ追加

**答え:**

```dart
Column(
  children: [
    Text('Item 1'),
    Text('Item 2'),
    Text('Item 3'),
  ],
)
```

---

## レベル2: 応用演習（ガイドを見ずに挑戦）

### 問題2-1: スタイル付きテキスト

**要件:**

- 画面中央に「Styled Text」というテキストを表示する
- フォントサイズを24、太字、色を青にする

**ヒント:**

- Textのstyleプロパティを使用
- TextStyleを使用

**答え:**

```dart
Center(
  child: Text(
    'Styled Text',
    style: TextStyle(
      fontSize: 24,
      fontWeight: FontWeight.bold,
      color: Colors.blue,
    ),
  ),
)
```

---

### 問題2-2: 入力欄とボタン

**要件:**

- テキスト入力欄と「送信」ボタンを縦に並べて表示する
- 入力欄には「名前」というラベルを表示する

**ヒント:**

- Columnを使用
- TextFieldとElevatedButtonを使用
- InputDecorationでラベルを設定

**答え:**

```dart
Column(
  children: [
    TextField(
      decoration: InputDecoration(
        labelText: '名前',
      ),
    ),
    ElevatedButton(
      onPressed: () {},
      child: Text('送信'),
    ),
  ],
)
```

---

### 問題2-3: 余白を追加

**要件:**

- テキストの周りに16ピクセルの余白を追加する
- テキストは「Padded Text」

**ヒント:**

- Paddingを使用
- EdgeInsets.allを使用

**答え:**

```dart
Padding(
  padding: EdgeInsets.all(16),
  child: Text('Padded Text'),
)
```

---

## レベル3: 実践演習（ガイドを見ずに挑戦）

### 問題3-1: カード形式の表示

**要件:**

- カードの中にタイトルと説明文を表示する
- タイトルは「Card Title」、説明文は「This is a card description」
- カードには角丸と影を追加する

**ヒント:**

- Cardを使用
- Columnで縦に並べる
- BoxDecorationで装飾

**答え:**

```dart
Card(
  elevation: 4,
  shape: RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(12),
  ),
  child: Padding(
    padding: EdgeInsets.all(16),
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          'Card Title',
          style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
        ),
        SizedBox(height: 8),
        Text('This is a card description'),
      ],
    ),
  ),
)
```

---

### 問題3-2: リスト表示

**要件:**

- 5つのアイテムをリスト形式で表示する
- 各アイテムは「Item 1」〜「Item 5」
- ListTileを使用してアイコンとテキストを表示する

**ヒント:**

- ListViewを使用
- ListTileを使用
- leadingにアイコンを設定

**答え:**

```dart
ListView.builder(
  itemCount: 5,
  itemBuilder: (context, index) {
    return ListTile(
      leading: Icon(Icons.list),
      title: Text('Item ${index + 1}'),
    );
  },
)
```

---

### 問題3-3: カウンターアプリ

**要件:**

- カウンターの値を表示するテキスト
- 値を増やすボタン
- 値を減らすボタン
- ボタンを押すと値が変化する

**ヒント:**

- StatefulWidgetを使用
- setStateで状態を更新
- 変数を定義して値を保持

**答え:**

```dart
class CounterApp extends StatefulWidget {
  @override
  _CounterAppState createState() => _CounterAppState();
}

class _CounterAppState extends State<CounterApp> {
  int _counter = 0;

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text(
          'Counter: $_counter',
          style: TextStyle(fontSize: 32),
        ),
        SizedBox(height: 16),
        Row(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              onPressed: () {
                setState(() {
                  _counter--;
                });
              },
              child: Text('-'),
            ),
            SizedBox(width: 16),
            ElevatedButton(
              onPressed: () {
                setState(() {
                  _counter++;
                });
              },
              child: Text('+'),
            ),
          ],
        ),
      ],
    );
  }
}
```

---

## レベル4: 応用演習（複合的なUI）

### 問題4-1: ログイン画面

**要件:**

- メールアドレス入力欄
- パスワード入力欄（非表示）
- ログインボタン
- 全体を中央揃えで表示
- 余白を適切に設定

**ヒント:**

- Columnで縦に並べる
- TextFieldを使用
- obscureTextでパスワードを非表示
- SizedBoxで余白を追加

**答え:**

```dart
Padding(
  padding: EdgeInsets.all(24),
  child: Column(
    mainAxisAlignment: MainAxisAlignment.center,
    children: [
      TextField(
        decoration: InputDecoration(
          labelText: 'メールアドレス',
          border: OutlineInputBorder(),
        ),
      ),
      SizedBox(height: 16),
      TextField(
        obscureText: true,
        decoration: InputDecoration(
          labelText: 'パスワード',
          border: OutlineInputBorder(),
        ),
      ),
      SizedBox(height: 24),
      SizedBox(
        width: double.infinity,
        child: ElevatedButton(
          onPressed: () {},
          child: Text('ログイン'),
        ),
      ),
    ],
  ),
)
```

---

### 問題4-2: TODOリスト

**要件:**

- テキスト入力欄
- 追加ボタン
- TODOアイテムのリスト表示
- アイテムを追加できる

**ヒント:**

- StatefulWidgetを使用
- Listでアイテムを管理
- ListView.builderでリスト表示
- setStateでリストを更新

**答え:**

```dart
class TodoApp extends StatefulWidget {
  @override
  _TodoAppState createState() => _TodoAppState();
}

class _TodoAppState extends State<TodoApp> {
  final _controller = TextEditingController();
  List<String> _todos = [];

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Padding(
          padding: EdgeInsets.all(16),
          child: Row(
            children: [
              Expanded(
                child: TextField(
                  controller: _controller,
                  decoration: InputDecoration(
                    labelText: 'TODOを入力',
                  ),
                ),
              ),
              SizedBox(width: 16),
              ElevatedButton(
                onPressed: () {
                  setState(() {
                    _todos.add(_controller.text);
                    _controller.clear();
                  });
                },
                child: Text('追加'),
              ),
            ],
          ),
        ),
        Expanded(
          child: ListView.builder(
            itemCount: _todos.length,
            itemBuilder: (context, index) {
              return ListTile(
                title: Text(_todos[index]),
              );
            },
          ),
        ),
      ],
    );
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }
}
```

---

## レベル5: チャレンジ演習（ガイドなしで完全に自力で）

### 問題5-1: プロフィール画面

**要件:**

- 円形のアバター画像（またはイニシャル）
- 名前テキスト
- メールアドレステキスト
- 自己紹介テキスト
- 編集ボタン
- 適切な余白とスタイル

**ヒント:**

- CircleAvatarを使用
- Cardで囲む
- Columnで縦に並べる

---

### 問題5-2: 設定画面

**要件:**

- 複数の設定項目をリスト表示
- 各項目はスイッチまたは選択肢
- セクションごとにグループ化
- 適切なアイコンと説明

**ヒント:**

- ListViewを使用
- ListTileを使用
- SwitchまたはCheckboxを使用
- Dividerでセクションを区切る

---

### 問題5-3: 画像ギャラリー

**要件:**

- グリッド形式で画像を表示
- 2列配置
- 各画像はタップ可能
- タップすると詳細を表示

**ヒント:**

- GridView.countを使用
- crossAxisCountを2に設定
- GestureDetectorでタップ検知

---

## 繰り返し練習のための課題

### 課題1: 同じUIを3回作る

**目標:**

- 問題2-1のスタイル付きテキストを3回作る
- 1回目: ガイドを見ながら
- 2回目: ガイドを見ずに
- 3回目: 完全に暗記で

### 課題2: 入力欄とボタンの組み合わせを3回作る

**目標:**

- 問題2-2の入力欄とボタンを3回作る
- 毎回少しスタイルを変えてみる

### 課題3: カード形式の表示を3回作る

**目標:**

- 問題3-1のカードを3回作る
- 毎回異なる内容で作る

---

## 学習の進め方

### ステップ1: ガイドを見ながら理解する

- まずは答えを見て理解する
- 各行が何をしているか確認
- なぜそのウィジェットを使うか理解

### ステップ2: ガイドを見ずに挑戦する

- 答えを隠して自分で書く
- うまくいかない箇所をメモ
- 答えと比較して違いを確認

### ステップ3: 繰り返し練習する

- 同じ問題を複数回解く
- 毎回少し変化を加える
- 徐々に難易度を上げる

### ステップ4: 自分で課題を作る

- やりたいUIを想像する
- そのUIを実装してみる
- 必要に応じてガイドを参照

---

## 成功の基準

### レベル1-2: ガイドを見ながら書ける

- ガイドを参照しながら正解にたどり着ける

### レベル3: ガイドを見ずに書ける

- ガイドを見ずに正解にたどり着ける
- 時間はかかっても良い

### レベル4-5: スムーズに書ける

- ガイドを見ずにスムーズに書ける
- ウィジェット名が自然に出てくる

---

**ナビゲーション:**

- ← 前へ: [プロパティの理由付けガイド](./プロパティの理由付けガイド.md)
- [学習教材インデックスに戻る](./INDEX.md)
- 次へ: [コーディング思考プロセスガイド](./コーディング思考プロセスガイド.md) →

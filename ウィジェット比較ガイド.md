# ウィジェット比較ガイド

## このガイドの目的

このガイドは、似たような機能を持つウィジェットの違いを理解して、適切な場面で適切なウィジェットを選べるようにするためのものです。「どのウィジェットを使えばいいか分からない」という悩みを解決するために、各ウィジェットの特徴と使い分けを説明します。

---

## ボタン系ウィジェットの比較

### ElevatedButton vs TextButton vs IconButton

#### ElevatedButton

**特徴:**

- 影付きの立体的なボタン
- 主要なアクションに使用
- 目立つデザイン

**使用例:**

```dart
ElevatedButton(
  onPressed: () {},
  child: Text('送信'),
)
```

**適した場面:**

- フォームの送信ボタン
- 主要なアクション
- ユーザーに強調したい操作

**適さない場面:**

- 補助的な操作
- スペースが限られている場合

---

#### TextButton

**特徴:**

- テキストのみのシンプルなボタン
- 補助的なアクションに使用
- 目立たないデザイン

**使用例:**

```dart
TextButton(
  onPressed: () {},
  child: Text('キャンセル'),
)
```

**適した場面:**

- キャンセルボタン
- 補助的なアクション
- リンクのような操作

**適さない場面:**

- 主要なアクション
- 強調が必要な操作

---

#### IconButton

**特徴:**

- アイコンのみのボタン
- ツールバーアクションに使用
- コンパクトなデザイン

**使用例:**

```dart
IconButton(
  icon: Icon(Icons.favorite),
  onPressed: () {},
)
```

**適した場面:**

- ツールバーのボタン
- アイコンだけで意味が通じる操作
- スペースが限られている場合

**適さない場面:**

- テキストが必要な操作
- 説明が必要なアクション

---

### FloatingActionButton

**特徴:**

- 画面右下に浮いた円形のボタン
- 主要なアクションに使用
- Scaffoldのプロパティとして使用

**使用例:**

```dart
Scaffold(
  floatingActionButton: FloatingActionButton(
    onPressed: () {},
    child: Icon(Icons.add),
  ),
)
```

**適した場面:**

- 新規作成アクション
- 画面全体に関わる主要な操作
- 常に表示したいアクション

**適さない場面:**

- 補助的な操作
- 特定のコンテキストでのみ必要な操作

---

## テキスト入力系ウィジェットの比較

### TextField vs TextFormField

#### TextField

**特徴:**

- 基本的なテキスト入力フィールド
- シンプルで使いやすい
- コントローラーで値を管理

**使用例:**

```dart
TextField(
  controller: _controller,
  decoration: InputDecoration(
    labelText: '名前',
  ),
)
```

**適した場面:**

- シンプルな入力フィールド
- バリデーションが不要な場合
- 個別の入力フィールド

**適さない場面:**

- フォーム全体のバリデーションが必要な場合
- 複数の入力フィールドを一括管理したい場合

---

#### TextFormField

**特徴:**

- フォームバリデーション機能付き
- Formウィジェットと組み合わせて使用
- バリデーションエラーの表示が容易

**使用例:**

```dart
Form(
  key: _formKey,
  child: TextFormField(
    controller: _controller,
    validator: (value) {
      if (value == null || value.isEmpty) {
        return '必須項目です';
      }
      return null;
    },
  ),
)
```

**適した場面:**

- フォーム全体のバリデーションが必要
- 複数の入力フィールドを一括管理
- エラー表示が必要な場合

**適さない場面:**

- シンプルな入力フィールド
- バリデーションが不要な場合

---

## レイアウト系ウィジェットの比較

### Column vs Row

#### Column

**特徴:**

- 子ウィジェットを縦方向に配置
- mainAxisAlignmentで縦方向の配置を制御
- crossAxisAlignmentで横方向の配置を制御

**使用例:**

```dart
Column(
  children: [
    Text('Item 1'),
    Text('Item 2'),
    Text('Item 3'),
  ],
)
```

**適した場面:**

- 縦に並べる要素
- フォームの入力フィールド
- リスト形式の表示

**適さない場面:**

- 横に並べる要素
- 水平方向のスクロールが必要な場合

---

#### Row

**特徴:**

- 子ウィジェットを横方向に配置
- mainAxisAlignmentで横方向の配置を制御
- crossAxisAlignmentで縦方向の配置を制御

**使用例:**

```dart
Row(
  children: [
    Icon(Icons.home),
    Text('Home'),
    Icon(Icons.settings),
    Text('Settings'),
  ],
)
```

**適した場面:**

- 横に並べる要素
- アイコンとテキストの組み合わせ
- ナビゲーションバー

**適さない場面:**

- 縦に並べる要素
- 垂直方向のスクロールが必要な場合

---

### Stack vs Positioned

#### Stack

**特徴:**

- 子ウィジェットを重ねて配置
- 基本的な重ね合わせに使用
- alignmentで基本位置を制御

**使用例:**

```dart
Stack(
  children: [
    Image.network('url'),
    Text('Overlay'),
  ],
)
```

**適した場面:**

- 背景画像の上にテキストを配置
- 重ね合わせが必要なUI
- シンプルな絶対位置配置

**適さない場面:**

- 複雑な絶対位置配置
- 複数の要素を精密に配置したい場合

---

#### Positioned

**特徴:**

- Stack内で絶対位置を指定
- 詳細な位置制御が可能
- top, bottom, left, rightで位置を指定

**使用例:**

```dart
Stack(
  children: [
    Image.network('url'),
    Positioned(
      bottom: 16,
      left: 16,
      child: Text('Overlay'),
    ),
  ],
)
```

**適した場面:**

- 詳細な絶対位置配置
- Stack内での精密な位置制御
- 複雑な重ね合わせ

**適さない場面:**

- Stackの外で使用（エラーになる）
- シンプルな重ね合わせ

---

## コンテナ系ウィジェットの比較

### Container vs Padding vs SizedBox

#### Container

**特徴:**

- 汎用的なコンテナ
- サイズ、余白、装飾を一括管理
- 最も柔軟性が高い

**使用例:**

```dart
Container(
  width: 200,
  height: 100,
  padding: EdgeInsets.all(16),
  decoration: BoxDecoration(
    color: Colors.blue,
  ),
  child: Text('Hello'),
)
```

**適した場面:**

- 複数のスタイルを適用したい場合
- サイズと装飾を同時に指定
- 柔軟なレイアウトが必要な場合

**適さない場面:**

- 単純な余白のみの場合（Paddingで十分）
- 単純なサイズのみの場合（SizedBoxで十分）

---

#### Padding

**特徴:**

- 余白のみを追加
- シンプルで軽量
- Containerよりパフォーマンスが良い

**使用例:**

```dart
Padding(
  padding: EdgeInsets.all(16),
  child: Text('Hello'),
)
```

**適した場面:**

- 余白のみが必要な場合
- パフォーマンスを重視する場合
- シンプルな余白調整

**適さない場面:**

- サイズ指定が必要な場合
- 装飾が必要な場合

---

#### SizedBox

**特徴:**

- 固定サイズを指定
- スペーサーとして使用
- シンプルで軽量

**使用例:**

```dart
// スペーサーとして使用
Column(
  children: [
    Text('Top'),
    SizedBox(height: 20),
    Text('Bottom'),
  ],
)

// サイズ固定として使用
SizedBox(
  width: 100,
  height: 100,
  child: Text('Fixed'),
)
```

**適した場面:**

- 固定サイズが必要な場合
- スペーサーとして使用
- シンプルなサイズ指定

**適さない場面:**

- 余白が必要な場合（Paddingを使用）
- 装飾が必要な場合（Containerを使用）

---

## リスト系ウィジェットの比較

### ListView vs GridView vs SingleChildScrollView

#### ListView

**特徴:**

- スクロール可能なリスト
- 効率的なアイテム生成
- builderで動的リストに対応

**使用例:**

```dart
ListView.builder(
  itemCount: 100,
  itemBuilder: (context, index) {
    return ListTile(title: Text('Item $index'));
  },
)
```

**適した場面:**

- 多数のアイテムを表示
- スクロールが必要なリスト
- 動的なリスト

**適さない場面:**

- 少数のアイテムのみ（Columnで十分）
- グリッドレイアウト（GridViewを使用）

---

#### GridView

**特徴:**

- グリッド形式のリスト
- 2次元のレイアウト
- カード形式の表示に適している

**使用例:**

```dart
GridView.count(
  crossAxisCount: 2,
  children: [
    Container(color: Colors.red),
    Container(color: Colors.blue),
  ],
)
```

**適した場面:**

- グリッドレイアウト
- カード形式の表示
- 画像ギャラリー

**適さない場面:**

- 縦方向のリスト（ListViewを使用）
- 横方向のリスト（ListViewを使用）

---

#### SingleChildScrollView

**特徴:**

- 単一の子ウィジェットをスクロール可能に
- シンプルなスクロール
- ColumnやRowと組み合わせて使用

**使用例:**

```dart
SingleChildScrollView(
  child: Column(
    children: [
      Text('Item 1'),
      Text('Item 2'),
      // 多くのアイテム...
    ],
  ),
)
```

**適した場面:**

- 少数のアイテムでスクロールが必要
- シンプルなスクロール
- フォーム全体のスクロール

**適さない場面:**

- 多数のアイテム（ListViewを使用）
- 効率的なアイテム生成が必要な場合

---

## ナビゲーション系ウィジェットの比較

### Navigator.push vs Navigator.pushNamed

#### Navigator.push

**特徴:**

- 直接画面遷移を指定
- 動的な画面遷移に対応
- データの受け渡しが容易

**使用例:**

```dart
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SecondScreen()),
);
```

**適した場面:**

- 動的な画面遷移
- データを渡す必要がある場合
- シンプルな画面遷移

**適さない場面:**

- 多数の画面遷移（pushNamedで管理）
- ルート名で管理したい場合

---

#### Navigator.pushNamed

**特徴:**

- ルート名で画面遷移
- MaterialAppでルートを定義
- 一元管理が可能

**使用例:**

```dart
// MaterialAppでルート定義
MaterialApp(
  routes: {
    '/': (context) => HomeScreen(),
    '/details': (context) => DetailsScreen(),
  },
)

// 画面遷移
Navigator.pushNamed(context, '/details');
```

**適した場面:**

- 多数の画面遷移
- ルート名で管理したい場合
- 大規模なアプリ

**適さない場面:**

- 動的な画面遷移
- データを渡す必要がある場合

---

## 選択の判断フロー

### ボタンを選ぶ際の判断フロー

```
何を表示したいか？
├── アイコンのみ → IconButton
├── テキストのみ
│   ├── 主要なアクション → ElevatedButton
│   └── 補助的なアクション → TextButton
└── 画面右下の主要アクション → FloatingActionButton
```

### レイアウトを選ぶ際の判断フロー

```
どう配置したいか？
├── 縦に並べる → Column
├── 横に並べる → Row
├── 重ねて配置
│   ├── シンプルに → Stack
│   └── 詳細に → Positioned
└── スクロールが必要
    ├── リスト形式 → ListView
    ├── グリッド形式 → GridView
    └── シンプルに → SingleChildScrollView
```

### コンテナを選ぶ際の判断フロー

```
何が必要か？
├── 余白のみ → Padding
├── サイズのみ → SizedBox
└── 複数のスタイル → Container
```

---

## 練習問題

### 問題1: 次の場合、どのボタンを使用すべきか？

「キャンセル」ボタンを作成したい。主要なアクションではなく、補助的な操作。

### 問題2: 次の場合、どのレイアウトを使用すべきか？

3つのテキストを横に並べたい。アイコンとテキストの組み合わせ。

### 問題3: 次の場合、どのコンテナを使用すべきか？

テキストの周りに16ピクセルの余白を追加したい。サイズや装飾は不要。

---

**ナビゲーション:**

- ← 前へ: [ウィジェットガイド](./ウィジェットガイド.md)
- [学習教材インデックスに戻る](./INDEX.md)
- 次へ: [プロパティの理由付けガイド](./プロパティの理由付けガイド.md) →

# トラブルシューティングガイド

## 1. 共通基盤のリセット手順

動作が不安定になった時や、予期せぬビルドエラーが起きた時は、以下の手順で環境のクリーンアップを行ってください。

```bash
# 1. ビルドキャッシュの削除
fvm flutter clean

# 2. 依存関係の再取得
fvm flutter pub get

# 3. 現状の環境診断
fvm flutter doctor
```

---

## 2. 環境構築時の主な問題と解決策

### 2-1. コマンドが見つからない（command not found）

- **原因**: 環境変数（PATH）の設定が不十分な可能性があります。
- **対策**:
  - 全てのコマンドの冒頭に `fvm` を付加しているか確認してください（例: `fvm flutter run`）。
  - [環境構築ガイド](./開発環境構築ガイド.md) の「実行パスの設定」を再度確認し、シェルを再起動してください。

### 2-2. Windows環境でのAndroid SDKエラー

- **原因**: Android Studioの内部ツールが未インストールである可能性があります。
- **対策**:
  1. Android Studio の「SDK Manager」を起動します。
  2. 「SDK Tools」タブ内の `Android SDK Command-line Tools (latest)` にチェックを入れ、インストールを完了させてください。

---

## 3. 実行時の主な問題と解決策

### 3-1. RenderFlex overflowed (画面のはみ出し)

- **物理的症状**: 画面端に黄色と黒の縞模様が表示されます。
- **技術的原因**: ウィジェットが親要素のサイズ制限を超えて描画されようとしています。
- **対策**:
  - はみ出しているウィジェットを `Expanded` または `Flexible` でラップして制約を与えるか、`SingleChildScrollView` を導入してスクロール可能にしてください。

### 3-2. Android Emulator が認識されない

- **対策**:
  - Android Studio の「Device Manager」から仮想デバイスを明示的に起動してください。
  - **IDEの確認**: エディタ右下のデバイス選択メニューから、正しいターゲットが選択されているか確認してください。
  - **エミュレータ再起動**: エミュレータが起動しているにもかかわらず認識されない場合は、エミュレータを再起動してください。

---

## 4. SSH認証に関するエラー

### 4-1. Permission denied (publickey)

- **原因**: 生成した公開鍵がGitHubに未登録であるか、秘密鍵が正しく読み込まれていません。
- **対策**:
  - 鍵の生成手順を再確認し、`cat ~/.ssh/id_ed25519.pub` で表示される内容が [GitHubのSSH設定](https://github.com/settings/ssh/new) と一致しているか確認してください。

> [!TIP]
> **効率的な技術調査の手法**
> エラーログをIDEから詳細に抽出、コピーし、AI（Cursor等）に入力することで、具体的な修正提案を受けることができます。また、エラーメッセージを直接検索エンジンで検索し、最新の解決事例を参照することも重要です。

---

## 5. コーディング時の主な問題と解決策

### 5-1. ウィジェットが見つからない（Undefined name）

**エラー例:**

```
Undefined name 'Text'
Undefined name 'Container'
```

**原因:**

- ウィジェット名のタイプミス
- Flutterのmaterialパッケージがインポートされていない

**対策:**

1. ウィジェット名のスペルを確認（大文字小文字の区別に注意）
2. ファイルの先頭に以下のインポートがあるか確認:
   ```dart
   import 'package:flutter/material.dart';
   ```
3. IDEの自動補完（Tabキー）を活用してタイプミスを防ぐ

### 5-2. プロパティが見つからない（The getter 'xxx' isn't defined）

**エラー例:**

```
The getter 'padding' isn't defined for the type 'Text'
The property 'color' can't be assigned to 'Container'
```

**原因:**

- ウィジェットがそのプロパティを持っていない
- プロパティ名のタイプミス

**対策:**

1. ウィジェットガイドでそのウィジェットが持つプロパティを確認
2. Cmd+Clickでウィジェットの定義を確認し、使用可能なプロパティを確認
3. 適切なウィジェットを使用しているか再検討

### 5-3. 型の不一致（型エラー）

**エラー例:**

```
The argument type 'String' can't be assigned to the parameter type 'int'
A value of type 'Null' can't be assigned to a variable of type 'String'
```

**原因:**

- プロパティに間違った型の値を渡している
- null安全の違反

**対策:**

1. プロパティが期待する型を確認（Cmd+Clickで定義を確認）
2. 文字列が必要な箇所に数値を渡していないか確認
3. null許容型（`String?`）と非null型（`String`）の違いを理解

### 5-4. setState()の誤用

**エラー例:**

```
setState() called after dispose()
```

**原因:**

- ウィジェットが破棄された後にsetStateを呼んでいる
- StatelessWidgetでsetStateを使用している

**対策:**

1. StatefulWidgetでのみsetStateを使用する
2. 非同期処理の後にsetStateを呼ぶ場合、ウィジェットがまだマウントされているか確認:
   ```dart
   if (mounted) {
     setState(() {
       // 状態更新
     });
   }
   ```

### 5-5. コンテキスト（context）のエラー

**エラー例:**

```
No Scaffold widget found
```

**原因:**

- Scaffoldの外でScaffold.of(context)を呼んでいる
- Builderを使用していない

**対策:**

1. Scaffold.of(context)を使用する場合、Builderでラップ:
   ```dart
   Scaffold(
     body: Builder(
       builder: (context) => ElevatedButton(
         onPressed: () {
           ScaffoldMessenger.of(context).showSnackBar(...);
         },
       ),
     ),
   )
   ```

---

## 6. デバッグの効率的な進め方

### 6-1. エラーメッセージの読み方

エラーメッセージには以下の情報が含まれています:

1. **エラーの種類**: 何が問題か（型不一致、未定義など）
2. **ファイル名と行番号**: どこでエラーが発生しているか
3. **期待される値**: 何が期待されているか

**読む順序:**

1. まずエラーの種類を理解
2. ファイル名と行番号で該当箇所を特定
3. 期待される値を確認し、現在のコードと比較

### 6-2. 最小限の再現コードを作成する

複雑なエラーの場合、問題を切り分けるために最小限のコードを作成します:

1. 問題の箇所だけを切り出して新しいファイルに作成
2. そのコードだけでエラーが再現するか確認
3. 原因を特定したら、元のコードに修正を適用

### 6-3. 公式ドキュメントの活用方法

1. Flutter公式サイトで該当ウィジェットを検索
2. 「Constructor」セクションで使用可能なプロパティを確認
3. 「Example」セクションで実装例を参照
4. APIドキュメントで詳細な仕様を確認

---

## 7. よくある実装ミスと修正例

### 7-1. Column/Rowのchildrenを忘れる

**間違った例:**

```dart
Column(
  Text('Item 1'),  // エラー: childrenプロパティがない
  Text('Item 2'),
)
```

**正しい例:**

```dart
Column(
  children: [
    Text('Item 1'),
    Text('Item 2'),
  ],
)
```

### 7-2. Textのdataプロパティを忘れる

**間違った例:**

```dart
Text('Hello')  // OK（省略形）
Text(data: 'Hello')  // OK（明示的な指定）
```

**注意:** Textは直接文字列を渡せるが、他のウィジェットではプロパティ名が必要

### 7-3. onPressedを忘れる

**間違った例:**

```dart
ElevatedButton(
  child: Text('Click'),  // ボタンが押せない
)
```

**正しい例:**

```dart
ElevatedButton(
  onPressed: () {
    // ボタンが押された時の処理
  },
  child: Text('Click'),
)
```

---

**ナビゲーション:**

- ← 前へ: なし
- [学習教材インデックスに戻る](./INDEX.md)
- 次へ: なし（参考資料）
